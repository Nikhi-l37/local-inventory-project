# Database Configuration
# For local development with PostgreSQL
# DATABASE_USER=postgres
# DATABASE_HOST=localhost
# DATABASE_NAME=local_inventory
# DATABASE_PASSWORD=your_password
# DATABASE_PORT=5432
# DATABASE_SSL=false

# For Supabase (Production - use DIRECT connection, NOT pooler)
# Get these values from your Supabase project settings -> Database -> Connection string (Direct)
DATABASE_USER=postgres
DATABASE_HOST=aws-0-xx-0000.pooler.aws-0-region-0.prod.supabase.co
DATABASE_NAME=postgres
DATABASE_PASSWORD=your_database_password
DATABASE_PORT=5432
DATABASE_SSL=true

# JWT Secret for authentication
# Generate a secure random string for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change_this_to_a_secure_random_string

# Note: Make sure to enable PostGIS extension in Supabase:
# 1. Go to your Supabase Dashboard
# 2. Navigate to Database -> Extensions
# 3. Enable "postgis" extension
# ============================================
# Supabase Configuration (Optional but recommended for service role access)
# ============================================
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ============================================
# SendGrid Configuration for OTP Email Verification
# ============================================
# Get your SendGrid API key:
# 1. Sign up at https://signup.sendgrid.com/ (Free tier: 100 emails/day)
# 2. Verify your email address
# 3. Go to Settings → API Keys
# 4. Click "Create API Key" with "Full Access"
# 5. Copy the API key (starts with SG.)
# 6. Verify sender identity in SendGrid dashboard

# SendGrid API Key (starts with SG.)
SENDGRID_API_KEY=SG.your_api_key_here

# Verified sender email (must be verified in SendGrid)
MAIL_FROM=noreply@yourdomain.com

# From email address for sending OTP emails
MAIL_FROM=noreply@localinventory.com

# ============================================
# OTP Configuration (Optional - uses defaults if not set)
# ============================================
# OTP expiry time in seconds (default: 300 = 5 minutes)
OTP_EXPIRY_SECONDS=300

# Resend cooldown in seconds (default: 30)
OTP_RESEND_COOLDOWN_SECONDS=30

# Maximum verification attempts before lockout (default: 5)
OTP_MAX_ATTEMPTS=5

# Maximum number of resends allowed per challenge (default: 3)
OTP_MAX_RESENDS=3